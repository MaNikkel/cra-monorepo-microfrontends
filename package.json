{
  "name": "@cmm/root",
  "private": true,
  "devDependencies": {
    "craco": "^0.0.3",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna exec --scope @cmm/container -- yarn start",
    "build": "lerna exec --scope @cmm/container -- yarn build",
    "serve": "lerna exec --scope @cmm/container -- serve -s build"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  }
}
